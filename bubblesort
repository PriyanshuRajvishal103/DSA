def bubblesort(arr):
    l = len(arr)
    for i in range(l):
        for j in range(0,l-i-1):
            if(arr[j]>arr[j+1]):
                arr[j],arr[j+1] = arr[j+1],arr[j]

    return arr

arr = [1,3,2,6,4,2,1]
bubblesort(arr);
print(arr)
